config:
  moduleDescription: "Error List screen. Displays the mistakes made by the user during the game. The screen consists of the following components: 1. A basic toolbar. 2. A list of errors, each item containing the question, correct answer, and a favorite icon."
  uiSemantics: "AI client screen with a toolbar including back and open-in-browser actions, displays Markdown content in a vertically scrollable column, handles loading and warning states with retry options, shows error messages via snackbars, and navigates back or to external web links."
  excludeTranslated: true
